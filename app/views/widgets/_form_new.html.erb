<%= simple_form_for [report, Widget.new], remote: true do |f| %>
  <%= f.input :grid_item_position, as: :hidden %>
  <%= f.association :kpi, input_html: { data: { action: "dashbuilder#newWidgetSubmit" } }, label: false %>
  <%= f.submit class: "hidden" %>
<% end %>
