
<h2> Total: <%=number_to_human(Order.joins(:order_items).sum(:quantity))%></h2>
<!--number_with_delimiter to display with decimal: ','  -->
<%= line_chart Order.joins(:order_items).group_by_month(:purchase_date).sum(:quantity), thousands: "," %>

