<%= column_chart KPI.revenue(interval: "day"), thousands: ",",
  library: {
    legend: {
      labels: {
        fontColor: '#FFFFFFCC'
      }
    },
    gridLines: {
      color: '#FFFFFFCC'
    },
    scales: {
      yAxes: [{
        ticks: {
          fontColor: '#FFFFFFCC'
        },
      }],
      xAxes: [{
        ticks: {
          fontColor: '#FFFFFFCC'
        }
      }]
    }
  }
%>
